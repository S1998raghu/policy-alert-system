apiVersion: v1
kind: Service
metadata:
  name: policy-alert-system
  namespace: default
  labels:
    app: policy-alert-system
spec:
  selector:
    app: policy-alert-system
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
